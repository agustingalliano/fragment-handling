<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="galliano.android.tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tools"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_tools_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Herramientas">
        <activity android:name="galliano.android.tools.ToolsActivity"></activity>
        <activity android:name="galliano.android.tools.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="galliano.android.tools.utilities.ServiceMusic"
            android:enabled="true">
        </service>

    </application>

    <uses-permission android:name="android.permission.CAMERA">

    </uses-permission>
    <uses-permission android:name="android.permission.FLASHLIGHT">

    </uses-permission>

    <uses-feature android:name="android.hardware.camera">
    </uses-feature>

    <uses-feature android:name="android.hardware.camera.flash">
    </uses-feature>

</manifest>